/*import Student from '../classes/Student';

var studentArr = [];

studentArr.push(new Student('A01020725', 'Saúl Labra', 100, 100, 100, 'Aprobado'));
studentArr.push(new Student('A01020123', 'Emilio Hernández', 100, 100, 100, 'Aprobado'));

function rootReducer(state = studentArr, {type, payload}) {
    switch(type) {
        default:
            console.log('entró al default');
            return state;
    }
}

export default rootReducer;*/